name: Ett fantasifullt namn

on:
  push:
    branches: [ main ]

jobs:
  react_on_push:
    runs-on: ubuntu-latest
    steps:
    - name: Print Working Directory
      run: pwd
    - name: List Files
      run: ls
